# Movie Search App 🎬

This is a simple movie search app using the OMDb API.

## 🔍 Overview

This app allows users to search for movies by title. It fetches data from the OMDb API and displays movie cards with the poster, title, and release year.

## ⚙️ Setup Instructions

1. Clone the repository:
   ```
   git clone https://github.com/your-username/your-repo-name.git
   ```
2. Open the folder.
3. Replace `YOUR_API_KEY` in `app.js` with your OMDb API key.
4. Open `index.html` in a browser.

## 🧪 How to Use

1. Enter a movie title in the input box.
2. Click the **Search** button.
3. View movie results as cards with posters.

## ⚠️ Challenges Faced

- Ensured graceful handling of invalid or missing API key.
- Managed display of missing poster images with a placeholder.
- Handled API response errors and empty results.

## 📦 API Used

- [OMDb API](http://www.omdbapi.com/)
